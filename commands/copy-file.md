---
description: Copy file contents to clipboard. Pass a file path as argument.
allowed-tools: [Bash]
---

# /copy-file

Copy the contents of a file to clipboard.

```bash
cat "$ARGUMENTS" | bash ~/.claude/plugins/clipboard/scripts/clipboard.sh copy
```
