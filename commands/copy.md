---
description: Copy text to clipboard. Pass text as argument or pipe via stdin.
allowed-tools: [Bash]
---

# /copy

Copy text to clipboard (cross-platform: macOS, Linux X11/Wayland, Windows).

```bash
bash ~/.claude/plugins/clipboard/scripts/clipboard.sh copy "$ARGUMENTS"
```
